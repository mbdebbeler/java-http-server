.gradle
/build/

#Ignore jar to ensure rebuilt jar from Gradle is the one that gets deployed
out/artifacts/java_http_server_jar/*.jar

# Ignore logger so local logs don't get mixed up with deployed logs
*.log.*
Logs/
logger.log

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

#DS_Store
.DS_Store

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties
